# IndecisionApp

To run : 
* `git clone`
* `cd indecision_app`
* `npm install`
* `npm install live-server -g`
* `babel src/app.js --out-file=public/scripts/app.js --presets=env,react --watch`
* `cd public`
* `live-server`
